# Set default behavior, in case users don't have core.autocrlf set.
* text=auto eol=lf

# Explicitly declare text files we want to always have LF line endings.
*.c text
*.h text
*.cmake text
*.txt text
*.md text
*.yml text
*.ps1 text

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.hex binary
